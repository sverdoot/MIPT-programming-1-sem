#include <stdio.h>
#include <assert.h>
#include <malloc.h>
#include <string.h>
#include "the tree.h"

/*int getN (char *buf)
    {
    assert (buf);
    if isdigit (*(buf+1))
        if

int gett

int getp

int gets
 */
